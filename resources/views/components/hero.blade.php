<div class="relative h-auto text-white bg-opacity-50 bg-center bg-cover"
    style="background-image: url('/img/homebanner.png')">
    <div class="w-full h-full px-10 py-16 bg-black bg-opacity-50">
        <div class="md:w-1/2">
            <p class="text-3xl font-bold">{{ __('Wishlist') }}</p>
            <p class="mb-10 text-xl leading-none">{{ __('Register to save your wishlist') }}</p>
            <a href="{{ route('register') }}"
                class="px-8 py-4 text-xs font-bold text-white uppercase rounded bg-primary-500 hover:bg-primary-600">{{ __('Register') }}
            </a>
        </div>
    </div>
</div>
